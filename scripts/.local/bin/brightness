#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
    --inc)
        brightnessctl set +5%
        ;;
    --dec)
        brightnessctl set 5%-
        ;;
    *)
        echo "Usage: brightness <--inc|--dec>"
        exit 1
        ;;
esac
